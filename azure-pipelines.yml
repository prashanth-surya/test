# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: scaled-agent

steps:
- checkout: none
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'Start-Sleep -s 180'
  displayName: 'wait 30 seconds'
